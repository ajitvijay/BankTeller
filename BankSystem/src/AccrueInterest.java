import javax.swing.*;
import java.awt.*;
import java.sql.*;

public class AccrueInterest {
	
	public AccrueInterest() {
		JFrame frame = new JFrame("Accrue Interest");
		frame.setSize(400,400);
		frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		frame.setVisible(true);
	}
}
